# Un container Docker contenant WordPress + php-fpm (installé et configuré) uniquement sans nginx

FROM debian:buster

# RUN apk update && \
#     apk add php-fpm &&;

# COPY ./config/default.conf /var/www/html

# quand t'as des env variables à utiliser faire un sh

